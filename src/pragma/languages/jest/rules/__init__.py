"""Jest-only rules. Most jest detection reuses the shared rule chain from
`pragma.languages.vitest.rules` parameterized by JEST_DIALECT; this package
holds only what's truly jest-specific (e.g. `test.failing`).
"""
