[project]
name = "pragma"
version = "2.1.7"
description = "Claude Code plugin that catches AI test-gaming."
readme = "README.md"
requires-python = ">=3.11"
license = "MIT"
authors = [{ name = "Pragma contributors" }]
dependencies = [
    "typer>=0.12",
    "tree-sitter>=0.23,<0.24",
    "tree-sitter-typescript>=0.23,<0.24",
    "coverage[toml]>=7.4",
]

[project.optional-dependencies]
dev = ["pytest>=8.0", "ruff>=0.6", "mypy>=1.10"]
coverage = ["pytest-timeout>=2.3"]
llm = ["openai>=1.40"]

[project.scripts]
pragma = "pragma.cli:app"

[project.urls]
Homepage = "https://github.com/Joncik91/pragma"
Repository = "https://github.com/Joncik91/pragma"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/pragma"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q --strict-markers --import-mode=importlib"

[tool.ruff]
line-length = 100
target-version = "py311"
# Fixtures are deliberately gamed test files; lint would "fix" the very
# patterns the classifier exists to detect.
extend-exclude = ["tests/fixtures/**"]

[tool.ruff.lint]
select = ["E", "F", "I", "B", "UP", "SIM"]

[tool.ruff.lint.per-file-ignores]
# SYSTEM_PROMPT contains intentionally long text lines for LLM caching;
# splitting them would change the string content and break prompt-cache hits.
"src/pragma/judge/prompt.py" = ["E501"]

[tool.mypy]
strict = true
python_version = "3.11"
files = ["src/pragma"]
