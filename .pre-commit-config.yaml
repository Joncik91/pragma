# Auto-generated by `pragma init` — see docs/superpowers/specs for design.
# v0.2 ships manifest + gate checks via `pragma verify all`.
# v0.4+ will add ruff / mypy / gitleaks / semgrep / pip-audit / deptry stages.
repos:
  - repo: local
    hooks:
      - id: pragma-verify-all
        name: pragma verify all
        entry: .venv/bin/python3 -m pragma verify all
        language: system
        pass_filenames: false
        always_run: true
        stages: [pre-commit]
