# Auto-generated by `pragma init` — see docs/superpowers/specs for design.
# v0.1 ships the minimal hook set: local-repo stub calling `pragma verify manifest`.
# v0.2+ will add ruff / mypy / gitleaks / semgrep / pip-audit / deptry stages.
repos:
  - repo: local
    hooks:
      - id: pragma-verify-manifest
        name: pragma verify manifest
        entry: .venv/bin/python3 -m pragma verify manifest
        language: system
        pass_filenames: false
        always_run: true
        stages: [pre-commit]
